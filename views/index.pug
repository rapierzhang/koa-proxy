extends layout

block content
  div.header node中转后台

  div.content
    div.sidebar
      ul.module-list
        each item in moduleList
          a.module-list-item
            li.module-item #{item}

      div.module-add +

    div.context
      iframe.context-iframe(src='./admin/module/urlList')